info:
  name: Delete product
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url}}/products/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
