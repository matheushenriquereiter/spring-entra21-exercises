info:
  name: Restock product
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}/products/:id/restock?quantity=1"
  params:
    - name: quantity
      value: "1"
      type: query
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
