info:
  name: Get product
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/products/:id"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
